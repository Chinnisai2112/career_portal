# Frontend API base URL (optional — dev proxy handles /api when omitted)
REACT_APP_API_URL=http://localhost:5000/api

# Dev server port (optional)
PORT=3000

# Set to none to skip auto-opening the browser on start
BROWSER=none
